<h2>A sample experiment: {{model.title}}</h2>

<div class="clearfix">
{{exp-player
    frames=model.experiment.structure
    saveHandler=(action 'saveSession')
    frameIndex=selectedFrame
}}
</div>

{{outlet}}
