import { moduleForComponent, test } from 'ember-qunit';
// import hbs from 'htmlbars-inline-precompile';

moduleForComponent('experiment-detail', 'Integration | Component | experiment detail', {
  integration: true
});

test('it renders', function(assert) {
    // TODO figure out how to test with ember-wormhole
    assert.ok(true);
});
