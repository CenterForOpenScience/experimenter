import { immutable } from '../../../helpers/immutable';
import { module, test } from 'qunit';

module('Unit | Helper | immutable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = immutable([42]);
  assert.ok(result);
});
