import { trim } from 'experimenter/helpers/trim';
import { module, test } from 'qunit';

module('Unit | Helper | trim');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trim([42]);
  assert.ok(result);
});
